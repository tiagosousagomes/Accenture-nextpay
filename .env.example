# Copie este arquivo para .env e preencha os valores antes de subir a aplicação
# cp .env.example .env

# Chave da API Gemini (Google AI Studio: https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=sua_chave_gemini_aqui

# Senha de App do Gmail (Configurações > Segurança > Senhas de app)
# O valor padrão já está configurado no application.properties; só altere se necessário
MAIL_PASSWORD=nzmcbmvyttfuvhit
